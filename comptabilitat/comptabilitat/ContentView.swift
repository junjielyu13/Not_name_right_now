//
//  ContentView.swift
//  comptabilitat
//
//  Created by junjieli on 31/3/25.
//

import SwiftUI

struct ContentView: View {
    
    let x = "";
    let y = 1;
    
    var body: some View     {
        VStack {
            Image(systemName: "globe")
                            .imageScale(.large)
                    .foregroundStyle(.tint)
            Text("Hello, world!")
                        }
            .padding()
        }
}

#Preview {
    ContentView()
}
